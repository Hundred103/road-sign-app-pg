{
    # Generated by scripts/setup-proxy.sh
    email mati1mich@gmail.com
}

# Public domain with Let's Encrypt
roadsigns.ftp.sh {
    encode gzip zstd
    reverse_proxy frontend:80
}

# Local fallback (no TLS)
localhost, 127.0.0.1 {
    encode gzip zstd
    reverse_proxy frontend:80
}
